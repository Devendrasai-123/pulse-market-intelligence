# Pulse API — production dependencies for Render / local.
# Install: pip install -r requirements.txt

fastapi==0.141.1
uvicorn[standard]==0.52.3
supabase==2.31.0
python-dotenv==1.2.3

# CrewAI + LiteLLM (NVIDIA NIM provider: nvidia_nim/...)
crewai[litellm]==1.15.16
