# Frontend env — copy to `.env` if you need to override the API URL.
# Never commit a file that contains secrets (this API URL is public/local).

# Backend origin used by src/api/client.js
VITE_API_URL=http://127.0.0.1:8000
