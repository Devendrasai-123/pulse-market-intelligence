# Pulse frontend

React + Vite terminal. From this folder:

```powershell
copy .env.example .env
npm install
npm run dev
```

`VITE_API_URL` defaults to `http://127.0.0.1:8000`. Full setup: [../README.md](../README.md).
